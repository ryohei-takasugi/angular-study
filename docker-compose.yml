services:
  angular-dev:
    build: .
    # volumes:
    #   - .:/workspace:cached
    #   - node_modules:/workspace/node_modules
    tty: true
    stdin_open: true
    ports:
      - "4200:4200"
    # command: /bin/sh -c "npm install && ng serve --host 0.0.0.0"

volumes:
  node_modules: